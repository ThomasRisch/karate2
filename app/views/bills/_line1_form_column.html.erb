<% scope ||= nil %>
<dl>
  <dt>
    <label for="<%= "record_#{column.name}" %>">
      <%= active_scaffold_config.columns[:line1].label %>
    </label>
  </dt>
  <dd>
    <%= form_column(active_scaffold_config.columns[:amount1], scope) %> 
    <%= form_column(active_scaffold_config.columns[:text1], scope) %>
  </dd>
</dl>