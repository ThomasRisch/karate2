<% scope ||= nil %>
<dl>
  <dt>
    <label for="<%= "record_#{column.name}" %>">
      <%= active_scaffold_config.columns[:line_total].label %>
    </label>
  </dt>
  <dd>
    <%= form_column(active_scaffold_config.columns[:total], scope) %> 
    <%= form_column(active_scaffold_config.columns[:issue_date], scope) %>
  </dd>
</dl>