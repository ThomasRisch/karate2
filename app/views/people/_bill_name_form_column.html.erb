<% scope ||= nil %>
<dl>
  <dt>
    <label for="<%= "record_#{column.name}" %>">
      <%= active_scaffold_config.columns[:bill_name].label %>
    </label>
  </dt>
  <dd>
    <%= form_column(active_scaffold_config.columns[:bill_lastname], scope) %> 
    <%= form_column(active_scaffold_config.columns[:bill_firstname], scope) %>
  </dd>
</dl>
